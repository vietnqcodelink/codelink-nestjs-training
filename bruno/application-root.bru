meta {
  name: Application root
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

tests {
  test("returns a successful response", function() {
    expect(res.status).to.equal(200);
  });

  test("returns a request ID", function() {
    expect(res.getHeaders()).to.have.property("x-request-id");
  });
}
